# 🏗️ Архитектура

Just Watch построен на микросервисной архитектуре.

## Сервисы

| Сервис | Технологии | Назначение |
|-------|-----------|-----------|
| **API Gateway** | Go + Echo | Единая точка входа, маршрутизация |
| **Metadata Service** | Go + gRPC + MongoDB | Управление метаданными видео |
| **Video Streaming Service** | Go + MinIO + Echo | Загрузка и стриминг видео |
| **Frontend** | Vue.js | Пользовательский интерфейс |

## Хранилища

- **MongoDB**: метаданные (название, статус, владелец).
- **MinIO**: объектное хранилище для видеофайлов.

## Коммуникация

- Внешние запросы: REST → API Gateway.
- Внутренние вызовы: gRPC между Video Service и Metadata Service.

## Стриминг

Поддержка HTTP Byte Serving (`Range`-запросы) для перемотки видео.